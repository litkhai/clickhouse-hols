# AWS Configuration
# aws_region = "ap-northeast-2"  # Defaults to AWS_REGION or AWS_DEFAULT_REGION env var

# Project Configuration
# project_name         = "chc-glue-integration"
# glue_database_name   = "clickhouse_iceberg_db"
# enable_glue_crawler  = true

# Use existing IAM Role (recommended if you have one)
# If set, Terraform will attach required policies to this existing role
# If null, Terraform will try to create a new role
existing_clickhouse_role_name = "clickhouse-role"

# External ID for role assumption security
# clickhouse_external_id = "clickhouse-external-id-12345"

# Tags
# tags = {
#   Project     = "ClickHouse-Glue-Integration"
#   ManagedBy   = "Terraform"
#   Environment = "Demo"
# }
