services:
  clickhouse-latest:
    image: clickhouse/clickhouse-server:latest
    container_name: clickhouse-latest
    hostname: clickhouse-latest
    ports:
      - "8123:8123"  # HTTP Interface
      - "9000:9000"   # TCP Interface
    volumes:
      - clickhouse_data_latest:/var/lib/clickhouse
      - clickhouse_logs_latest:/var/log/clickhouse-server
    environment:
      CLICKHOUSE_DB: default
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: ""
    restart: unless-stopped
    security_opt:
      - seccomp=/Users/kenlee/Documents/GitHub/clickhouse-hols/local/oss-mac-setup/seccomp-profile.json
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8123/ping"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s
    ulimits:
      nofile:
        soft: 262144
        hard: 262144

volumes:
  clickhouse_data_latest:
    driver: local
  clickhouse_logs_latest:
    driver: local

networks:
  default:
    name: clickhouse-network
    driver: bridge
