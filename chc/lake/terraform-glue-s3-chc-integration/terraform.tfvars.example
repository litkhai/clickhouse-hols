# AWS Region
# Note: AWS credentials (access key, secret key) are provided via environment variables
# Set them before running: export AWS_ACCESS_KEY_ID=... export AWS_SECRET_ACCESS_KEY=...
# Or use the deploy.sh script which will prompt for them

aws_region = "ap-northeast-2"  # Must match your ClickHouse Cloud region

# Optional Configuration
# project_name          = "chc-glue-integration"
# glue_database_name    = "clickhouse_iceberg_db"
